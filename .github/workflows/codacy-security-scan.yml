name: Code Security Scan

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '27 11 * * 5'

permissions:
  contents: read

jobs:
  security-scan:
    permissions:
      contents: read
      security-events: write
      actions: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Check compilation
        run: |
          python3 -m py_compile builder.py
          echo "✅ builder.py compiled successfully"

      - name: Run Bandit security scan
        run: |
          pip install bandit
          bandit -r . -f sarif -o results.sarif \
            --exclude './venv,./node_modules,./.pytest_cache,./.idea,__pycache__,tests/venv' || \
            echo '{"version":"2.1.0","runs":[{"tool":{"driver":{"name":"Bandit"}},"results":[]}]}' > results.sarif
        continue-on-error: true

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: results.sarif