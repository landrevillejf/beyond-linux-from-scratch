# LFS 13.0 — 8.31. Sed-4.9 (final)
name="sed"
version="4.9"
source="https://ftp.gnu.org/gnu/sed/sed-4.9.tar.xz"
desc="Stream editor"
deps="glibc"

build() {
    ./configure --prefix=/usr
    make -j"$(nproc)"
    make DESTDIR="$PKG" install
}
