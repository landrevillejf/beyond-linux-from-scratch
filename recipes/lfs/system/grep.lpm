# LFS 13.0 — 8.35. Grep-3.12 (final)
name="grep"
version="3.12"
source="https://ftp.gnu.org/gnu/grep/grep-3.12.tar.xz"
desc="Pattern matching utilities"
deps="glibc"

build() {
    ./configure --prefix=/usr
    make -j"$(nproc)"
    make DESTDIR="$PKG" install
}
