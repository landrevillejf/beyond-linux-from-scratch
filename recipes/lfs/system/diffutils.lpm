# LFS 13.0 — 8.60. Diffutils-3.12 (final)
name="diffutils"
version="3.12"
source="https://ftp.gnu.org/gnu/diffutils/diffutils-3.12.tar.xz"
desc="File comparison utilities"
deps="glibc"

build() {
    ./configure --prefix=/usr
    make -j"$(nproc)"
    make DESTDIR="$PKG" install
}
