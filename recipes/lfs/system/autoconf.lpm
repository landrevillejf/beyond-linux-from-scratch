# LFS 13.0 — 8.46. Autoconf-2.72
name="autoconf"
version="2.72"
source="https://ftp.gnu.org/gnu/autoconf/autoconf-2.72.tar.xz"
desc="Tools for producing configure scripts"
deps="perl m4"

build() {
    ./configure --prefix=/usr
    make -j"$(nproc)"
    make install DESTDIR="$PKG"
}
